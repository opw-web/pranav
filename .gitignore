# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Tailwind standalone binary (80MB; downloaded via scripts, not source)
lens/scripts/tailwindcss

# NOTE: app/static/css/app.css IS committed — Vercel serves it as a static asset
# (@vercel/static) and there is no Node build step in the Python serverless deploy.

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Logs / scratch
*.log

# Vercel local project link
lens/.vercel/
